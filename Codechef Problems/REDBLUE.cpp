// N -->Red Points    M -->Blue Points

/*  delete the minimum number of points so that it's possible to draw a line such that all remaining 
red points are on one side of the line while all remaining blue points are on the other side of the line. */



#include<iostream>
#include<stdlib.h>

using namespace std;
int main(){
   system("CLS");
   int t;       //No. of test cases
   cin>>t;
   for(int test_case=1;test_case<=t;test_case++){
      int N , M;       //No. of Red and Blue balls
      cin>>N>>M;
      long int x[N] , y[M];
      for(int i=0;i<N;i++){
         cin>>x[i];
      }
      for(int j=0;j<M;j++){
         cin>>y[j];
      }
      for(int i=0;i<N;i++){
         for()
      }
   }
}