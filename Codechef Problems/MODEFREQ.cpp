// N numbers in a list from 1 to n.
// If different no. have same frequency then the no. pair which have more no. associated frequency is the answer 
// If more than one modal frequency present then ans is smallest frequency

#include<iostream>
#include<stdlib.h>

using namespace std;



int main(){
   system("CLS");
   int t;         //No. of test cases 
   cin>>t; 
   for(int test_case=1;test_case<=t;test_case++){
      int N;      //No. of values in chef's assingnment
      cin>>N;
      int asig[N];
      for(int i=0;i<N;i++){
         cin>>asig[i];
      }
      
       


   }

   return 0;
}